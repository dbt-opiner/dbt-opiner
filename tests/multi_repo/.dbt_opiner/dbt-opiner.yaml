global:
  custom_opinions_source: local
  # git_repository: Only if custom_rules_sources is git
  ignore_opinions:
    - O002

sql:
  files: ".*/(models|macros)/.*"
  opinions_config:
    O002_keywords:
      - summary
      - granularity

#yaml:
#md:
